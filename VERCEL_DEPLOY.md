# Vercel 배포 시 데이터 저장 방법

## ⚠️ 중요: Vercel의 서버리스 환경

Vercel은 **서버리스 함수(Serverless Functions)**를 사용합니다. 이는:
- 각 요청마다 독립적인 환경에서 실행
- 파일 시스템이 **임시적**이며 영구 저장 불가능
- 함수 실행 후 파일 시스템이 초기화됨

따라서 현재의 파일 기반 저장(`/data/logs.json`)은 **Vercel에서 작동하지 않습니다**.

## ✅ 해결 방법: Vercel KV 사용

Vercel KV는 Vercel에서 제공하는 Redis 기반 키-값 저장소로, 서버리스 환경에 최적화되어 있습니다.

### 1. Vercel KV 설정

1. **Vercel 대시보드에서 KV 스토어 생성**
   - Vercel 프로젝트 → Storage → Create Database → KV 선택
   - 스토어 이름 지정 (예: `kwangsoo-logs`)

2. **환경 변수 자동 설정**
   - Vercel이 자동으로 환경 변수를 추가합니다:
     - `KV_REST_API_URL`
     - `KV_REST_API_TOKEN`
     - `KV_REST_API_READ_ONLY_TOKEN`

### 2. 패키지 설치

```bash
npm install @vercel/kv
```

### 3. 코드 수정

현재 코드는 환경 변수를 확인하여:
- **로컬 개발**: 파일 기반 저장 사용
- **Vercel 배포**: Vercel KV 사용

자동으로 전환됩니다.

## 🔍 데이터 확인 방법 (Vercel 배포 후)

### 방법 1: API 엔드포인트 사용

```
https://your-project.vercel.app/api/logs
```

### 방법 2: Vercel 대시보드

1. Vercel 대시보드 → 프로젝트 선택
2. Storage → KV 스토어 선택
3. 데이터 확인

### 방법 3: Vercel CLI

```bash
vercel kv:get logs --store=kwangsoo-logs
```

## 💰 비용

- **Hobby 플랜**: 무료 (제한적)
- **Pro 플랜**: 사용량에 따라 과금
- 자세한 내용: https://vercel.com/docs/storage/vercel-kv

## 🔄 다른 옵션들

### 1. Supabase (PostgreSQL)
- 무료 티어 제공
- SQL 데이터베이스
- 더 복잡한 쿼리 가능

### 2. MongoDB Atlas
- 무료 티어 제공
- NoSQL 데이터베이스
- 유연한 스키마

### 3. PlanetScale
- MySQL 호환
- 서버리스
- 무료 티어 제공

## 📝 현재 구현 상태

현재 코드는 **하이브리드 방식**을 사용합니다:
- 환경 변수 `KV_REST_API_URL`이 있으면 → Vercel KV 사용
- 없으면 → 파일 기반 저장 (로컬 개발용)

이렇게 하면 로컬 개발과 프로덕션 배포 모두에서 작동합니다!

